<!--page/index/pages/tearcherDetail/tearcherDetail.wxml-->
<wxs module="fn">
  module.exports = {
  num: function(num) {
  return num > 10000 ? (num / 10000).toFixed(1) + '万' : num
  }
  }
</wxs>
<template name="listItem">
  <view catchtap="detailTap" wx:for="{{ list }}" wx:key="index" class="item" data-item="{{ item }}">
    <view class="left">
      <image class="poster" mode="aspectFill" src="{{ item.image || item.indexCover}}"></image>
      <view class="chargeBox" wx:if="{{!item.isCharge}}">免费</view>
      <view class="chargeBox charge" wx:else>收费</view>
    </view>
    <view class="right">
      <rich-text class="line name" nodes="{{ item.title || item.liveName}}"></rich-text>
      <view class="tags" wx:if="{{item.tags[0]}}">
        <text wx:for="{{item.tags}}" wx:key="index" wx:for-item="tag">{{tag.tagName}}</text>
      </view>
      <text class="line info">
        <text class="dramaNum">{{item.count}}课时</text>
        <text>|</text>
        <text class="playNum">{{ fn.num(item.viewNum) }}人在学</text>
      </text>
    </view>
  </view>
</template>
<navigationBar id="navigationBar" wx:if="{{scroll > 0}}" title="伦素聪" back="{{ true }}" isIndex="{{false}}"></navigationBar>
<view class="tabBox" wx:if="{{showTab}}" style="position: fixed;top: {{top}}px">
  <view class="tab">
    <text>课程 2</text>
    <text></text>
  </view>
</view>
<scroll-view class="scrollView" scroll-y bindscroll="bindscroll">
  <view class="topBox" style="padding-top: {{ statusBarHeight + 10 }}px;">
    <image src="/images/back.png" class="back" bindtap="back"></image>
    <view class="msgBox">
      <image src="../../images/avatarNone.png"></image>
      <view class="nickBox">
        <text>伦素聪</text>
        <text>深圳老年大学</text>
      </view>
      <view class="btn">关注</view>
    </view>
    <view class="inro">
      有一位学者，如此点评教育：“如果有一天，一个年轻的学生说出的一个想法，不被周围轻易“否决”，而开始觉得十分谨慎的回复世界的繁华会计师大后方跨境电商风格和考虑即使对方就看见法国航空发动机是给看见对方规划了肯定就发生过和快点结束访华给
    </view>
    <view class="bottom">
      <text class="center"></text>
      <view class="numBox">
        <text>关注</text>
        <text>234</text>
      </view>
      <view class="numBox">
        <text>粉丝</text>
        <text>335</text>
      </view>
    </view>
  </view>
  <view class="tabBox" id="tabBox">
    <view class="tab">
      <text>课程 2</text>
      <text></text>
    </view>
  </view>
  <template is="listItem" data="{{list: lessonList}}"></template>
</scroll-view>