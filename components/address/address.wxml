<!--components/address/address.wxml-->
<view class="address" wx:if="{{showCard}}"  catchmove="{{true}}">
    <view class="address-bg">
        <view class="address-card">
            <view class="card-title">兑换中</view>
            <view class="card-body">
                <!-- 收货人 -->
                <view class="card-info">
                    <view class="info-title">收货人</view>
                    <input type="text" placeholder="请填写收货人"
                     value="{{userInfo.username}}" placeholder-class="input-placeholder"
                     bindblur="changeName" />
                </view>
                <!-- 手机号 -->
                <view class="card-info">
                    <view class="info-title">手机号码</view>
                    <input type="number" placeholder="请填写手机号"
                     value="{{userInfo.mobile}}" placeholder-class="input-placeholder" 
                     bindblur="changeMobile" />
                </view>
                <!-- 所在地区 -->
                <view class="card-info">
                    <view class="info-title">所在地区</view>
                    <!-- <input type="text" placeholder="请填写所在地区" placeholder-class="input-placeholder" /> -->
                    <picker mode="multiSelector" value="{{ multiIndex }}" range="{{ multiAddress }}">
                        <view class="placeholder">
                            <text wx:if="{{ !param.address[1] }}">请选择</text>
                            <view class="picker" wx:if="{{ param.address[1] }}">
                                {{ param.address[0] }}，{{ param.address[1] }}，{{ param.address[2] }}
                            </view>
                            <view class="change">
                                <text>修改</text>
                                <image class="icon" mode="aspectFill" src="{{ $state.imgHost }}/Return-black.png"></image>
                            </view>
                        </view>
                    </picker>
                </view>
                <!-- 详细地址 -->
                <view class="card-info">
                    <view class="info-address">详细地址</view>
                    <textarea class="info-area" value="{{userInfo.address}}" bindblur="changeAddress"
                     placeholder="请输入详细地址" placeholder-class="input-placeholder"></textarea>
                </view>
            </view>
            <view class="card-footer">注意：虚拟物品(话费卡\油卡\会员卡等)将充值到填写的手机号码上</view>
            <view class="crad-btn">
                <view class="cancel-btn" catchtap="cancel">取消</view>
                <view class="confirm-btn" catchtap="confirm">确认</view>
            </view>
        </view>
    </view>
</view>